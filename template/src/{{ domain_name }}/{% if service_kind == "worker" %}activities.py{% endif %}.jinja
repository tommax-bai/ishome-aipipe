"""Temporal activities：所有 IO 收口在此。

注册名唯一真源：ishome-contracts activities/registry.md（只增不改，新增走 PR 评审）。
命名：@activity.defn(name="kebab-case") + 函数名同词 snake_case 动词前置。
"""

from __future__ import annotations
