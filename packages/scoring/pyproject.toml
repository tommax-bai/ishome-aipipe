[project]
name = "scoring"
version = "0.1.0"
description = "打分器（scorer）端口与实现（变化轴 #6：打分维度）"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/scoring"]
