[project]
name = "adapters"
version = "0.1.0"
description = "各模型的提示词/出入参变换层（模型供应商轴的非 API 部分）"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adapters"]
